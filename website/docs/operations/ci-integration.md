---
sidebar_position: 1
---

# CI Integration

OCTANE can run as a standalone CLI in any CI system or through the
`octane-action` GitHub Action.

CI runs should persist reports and, when appropriate, cache OCTANE story
results between jobs.

For distribution and action details, see `docs/distribution.md`.

