---
sidebar_position: 1
---

# CLI Reference

The CLI reference is generated from the `octane` command tree.

Until the generated website reference is wired into CI, build local man
pages with:

```bash
make man
```

The repository also contains `docs/cli-reference.md`.

