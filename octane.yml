# OCTANE configuration — adjust csmsEndpoint before running.
# Run: go run ./cmd/octane run --csms-endpoint ws://localhost:9210

storiesDir:  scenarios/v16
ocppVersion: "1.6"
maxParallel: 1
parameters:
  connectorId: "1"
  valid_idTag: "AABBCC"
  connectionTimeOut: "30s"
  meterStart: "0"
  meterStop: "1000"
